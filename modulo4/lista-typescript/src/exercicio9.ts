const anagram = (word: string) => {

    const size = word.length
    let result = size

    for (let i = 1; i < size; i++) {
        result *= 1
    }
    return result
}

console.log(anagram("Labenu")) 